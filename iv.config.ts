export default {
    name: '{{APP_NAME}}',      // Replaced during provisioning
    slug: '{{APP_SLUG}}',      // Replaced during provisioning
    platform: {
        designSystem: true,       // Use @invertedapps/design
        shellSdk: true,           // Use @invertedapps/sdk
    },
};
